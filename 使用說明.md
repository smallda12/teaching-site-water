# 水資源與能源｜教學網　使用說明

綜合領域　115 學年度 第 1 學期　**第 4 單元　第 13～16 週**

純靜態網站，**雙擊 `index.html` 就能用**，不需安裝、不需網路（延伸學習區的外部影片除外）。

---

## 七個分區

| 分區 | 內容 |
|---|---|
| 🙋 點點名 | 課前點名與抽籤，**名單與照片只存在這台電腦**，不會上傳、不會進 GitHub |
| 🃏 圖詞輪播 | 12 張學習圖卡的詞與短句，可自動輪播 |
| 🎬 教學影片 | 主影片（11.1 分）＋ 5 支重點短片，右側清單可自由切換 |
| 📚 教材教學 | 五個重點的教學頁，每頁附語音；每個重點結束有 3 題形成性評量（答對才過） |
| 📝 評量區 | 15 題總評量，**答錯不重答**，標出正解後直接進下一題 |
| 🧠 自學區 | 學生自己看頁圖與圖卡，可放大 |
| 🔗 延伸學習 | 12 支外部影片（**需要網路**），全部是台灣官方機構的宣導與教學短片 |

## 教材數量

- 頁圖 **74 頁**（教學 32、評量 30、圖卡 12）
- 評量 **15 題**兩選一，正解序列 `BBABAABABBAABAB`（A 7 題／B 8 題）
- 學習圖卡 **12 張**（3:2，概念 9 ＋ 情境 3）
- 影片 **6 支**（主影片 1 ＋ 重點短片 5），1728×1080、24fps
- 語音 **92 支**（`zh-TW-HsiaoChenNeural`，語速 -25%）

## 五個重點

| 重點 | 標題 | 對應課程計畫 |
|---|---|---|
| 1 | 生活需要水，也需要能源 | 第 13 週／學期目標 4-1 |
| 2 | 資源來到我們身邊不容易 | 第 14 週／學期目標 4-1 |
| 3 | 節約用水與節約能源 | 第 15 週／學期目標 4-2 |
| 4 | 浪費會帶來的後果 | 第 16 週／學期目標 4-2、4-3 |
| 5 | 少用一次性，多重複使用 | 第 16 週／學期目標 4-4（減塑） |

> 🔴 **重點 5 是 2026-08-10 新增的**：舊版 62 頁完全沒有教到學期目標 4-4（減塑），
> 只有一頁提到「重複使用」沾到邊。補成 10 頁之後，頁數 62 → 74、圖卡 10 → 12 張。

## 延伸學習的 12 支影片

來源全部是台灣官方機構：**經濟部水利署**（4 支）、**台電影音網**（2 支）、
**花蓮縣環保局**（3 支）、**台灣自來水公司**、**台中市環保局**、均一教育平台、樂樂TV。
長度多為 1～4 分鐘，五個重點都有對應影片。

- 老師 2026-08-11 逐支確認過才放上去。
- 每學期開學前跑一次 `工具腳本\檢查延伸影片.py` 確認影片沒被下架
  （YouTube 對失效影片會回傳灰縮圖但 HTTP 200，**看縮圖會誤判，一定要用 oembed 驗**）。

## 教室版與公開版

網站會自動判斷：

- **用 `file://` 開啟（雙擊 index.html）＝ 教室版**，可以上傳學生照片，資料只留在這台電腦。
- **用 `http(s)://` 開啟（線上版）＝ 公開版**，**照片上傳功能自動關閉**。

🛑 學生姓名與照片存在瀏覽器的 localStorage／IndexedDB，**絕不寫進檔案、絕不進 GitHub**。

## 改版之後一定要做的事

**動了 `教學網站\` 裡的任何東西，`index.html` 底部的 `?v=` 就加 1。**

`資源版本` 與 `語音.版本` 都住在 `js/config.js` 裡，而 `config.js` 自己是用
`js/config.js?v=1` 載入的——只改它們而不跳 `?v=`，回訪者會繼續用快取裡的舊 `config.js`，
**等於「加版本號」這個動作本身失效**，新頁圖、語音、PDF 一張都不會生效。

驗收要在**瀏覽器**比對 `CONFIG.資源版本` 與 `config.js?v=` 是否一致，**看本機檔案不算驗過**。

## 要改教材內容

教材本體是單元資料夾的 `pages\`（74 張頁圖），網站素材全部是它的衍生檔。
改內容的正確流程寫在單元資料夾的 `教材使用說明書.md`，**不要直接改網站的 `assets\`**。
